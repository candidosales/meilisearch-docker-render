services:
  meilisearch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: meilisearch
    ports:
      - "7700:7700"
    environment:
      - MEILI_ENV=development
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY}
    restart: unless-stopped
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.meilisearch.rule=Host(`meilisearch.vps-wings.org`)"
      - "traefik.http.routers.meilisearch.entrypoints=websecure"
      - "traefik.http.routers.meilisearch.tls.certResolver=letsencrypt"
      - "traefik.http.services.meilisearch.loadbalancer.server.port=3000"
networks:
  dokploy-network:
    external: true